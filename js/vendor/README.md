# Fremde Dateien

* `three.module.js` — three.js r169, unveraendert aus dem npm-Paket `three@0.169.0`
  (`build/three.module.min.js`). Lizenz: MIT, siehe `LICENSE-three.txt`.

Absichtlich mitgeliefert statt per CDN geladen: das Spiel soll auch ohne
Internetverbindung starten und der Server bleibt ohne npm-Abhaengigkeiten.
